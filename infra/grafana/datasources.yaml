apiVersion: 1

datasources:
  - name: Prometheus (OTel)
    type: prometheus
    access: proxy
    url: http://otel-collector:8889
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"
